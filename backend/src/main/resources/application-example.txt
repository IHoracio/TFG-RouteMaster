# Nombre de la aplicacion
spring.application.name=ejemplo

# Configuracion de JPA/Hibernate
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Zona horaria para Jackson
spring.jackson.time-zone=Europe/Madrid

# Claves API (obten las tuyas en Google y OpenWeatherMap)
evolutivo.api_key_google=TU_CLAVE_API_GOOGLE_AQUI
evolutivo.api_key_openweather=TU_CLAVE_API_OPENWEATHERMAP_AQUI

# Clave secreta para cookies de autenticacion (genera una segura)
evolutivo.auth.secret-cookie-key=TU_CLAVE_SECRETA_AQUI

# Configuracion de logging (cambiado a INFO para menos verbosidad)
logging.level.root=INFO
logging.level.es.metrica.sept25.evolutivo=DEBUG  # Cambiado para cubrir service y controller
logging.level.org.springframework.security=INFO

# Puerto del servidor
server.port=8080

# Configuracion de base de datos MySQL (local o AWS RDS)
spring.datasource.url=jdbc:mysql://localhost:3306/ejemplo?createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=TU_USUARIO_DB_AQUI
spring.datasource.password=TU_CONTRASENA_DB_AQUI

# Cors
cors.allowed.origins=http://localhost:4200
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*